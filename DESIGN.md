# baiz **项目级 DESIGN.md** v1.0

> **落点**：本仓根 `DESIGN.md`（＝`integrate/web-20260919` 线上仓，项目级·**覆盖**全局 `~/.claude/DESIGN.md`）
> **将来两用**：①前端施工基线 ②**baiz 自身**做 UI 产出时的规范（可挂成 skill）
> **依据**：全局正本 ＋《baiz 界面审美体检 v1.0》（2026-09-19）＋ `ui-designer` skill 硬约束

## 0. 覆盖声明

本件为**项目级覆盖**：与全局正本冲突处**以本件为准**；本件未规定处**回落到全局正本**。

## 1. 哲学（不变·四个词）

**克制 · 清晰 · 一致 · 触手可及**。像一本排印精良的书：没有多余元素，但每个元素都经过精确计算。

## 2. 调色板（**亮/暗双模·全部走 token·禁裸 hex**）

| 语义 token | 亮色 | 暗色 | 用途 |
|---|---|---|---|
| `--bg` | `#F8F9FA` | `#0F1117` | 页面底色 |
| `--surface` | `#FFFFFF` | `#1A1D27` | 卡片／面板／对话框 |
| `--surface-2` | `#F1F3F5` | `#232733` | 次级面（表头／代码块底） |
| `--text-primary` | `#1A1C1E` | `#E6E8EC` | 标题／正文 |
| `--text-secondary` | `#586270` | `#8B949E` | 辅助说明／时间戳 |
| `--text-disabled` | `#9CA3AF` | `#484F58` | 禁用态 |
| `--border` | `#E5E7EB` | `#30363D` | 分割线／输入框边框 |
| `--border-strong` | `#D1D5DB` | `#3F4753` | 需要强调的分隔 |
| `--accent` | `#4F46E5` | `#818CF8` | **唯一强调色**：主按钮／链接／选中态 |
| `--accent-hover` | `#4338CA` | `#6366F1` | hover |
| `--accent-soft` | `rgba(79,70,229,.10)` | `rgba(129,140,248,.16)` | 选中底／徽章底 |
| `--danger` | `#DC2626` | `#F87171` | 删除／错误 |
| `--success` | `#16A34A` | `#4ADE80` | 完成／对勾 |
| `--warning` | `#F59E0B` | `#FBBF24` | 警告／中风险 |

**baiz 专用语义（新增·同表维护）**

| 语义 | 亮色 | 暗色 | 用途 |
|---|---|---|---|
| `--risk-high` | `--danger` | `--danger` | 审批卡高风险徽章 |
| `--risk-medium` | `--warning` | `--warning` | 中风险徽章 |
| `--risk-unknown` | `--text-secondary` | `--text-secondary` | **档位未知**（不得伪装 medium） |
| `--diff-add-bg` | `rgba(22,163,74,.10)` | `rgba(74,222,128,.14)` | diff 增行 |
| `--diff-del-bg` | `rgba(220,38,38,.10)` | `rgba(248,113,113,.14)` | diff 删行 |
| `--code-bg` | `--surface-2` | `--surface-2` | 代码块／命令 |

**护栏**：正文对比度 ≥4.5:1；**不用纯黑/纯白做正文**；**一页一个强调色**；红仅 error/danger、绿仅 success。

## 3. 排版

```
正文: system-ui, -apple-system, "Segoe UI", "Noto Sans SC", sans-serif
等宽: "Cascadia Code", "Fira Code", "JetBrains Mono", Consolas, monospace
```

| 层级 | 字号 | 行高 | 用途 |
|---|---|---|---|
| h1 | 28 | 1.3 | 页面标题 |
| h2 | 22 | 1.3 | 区块标题 |
| h3 | 18 | 1.4 | 子标题／卡标题 |
| body | 15 | 1.6 | 正文（**中文行高 ≥1.6**） |
| small | 13 | 1.5 | 辅助、标签、卡内次要文本 |
| caption | 11 | 1.4 | 脚注／时间戳 |

**一屏 ≤3 种字号**；等宽**仅**用于代码／命令／数据。

## 4. 间距（**4px 基准·全 4 倍数**）

阶梯 **4 / 8 / 12 / 16 / 24 / 32 / 48 / 64**｜卡片内边距 **16（紧凑）/ 24（标准）**｜列表项 **8**｜按钮内边距 **8×16**（小号 6×12）｜输入框 **8×12**。
**标题与其内容间距 ≤ 标题与上方间距**（接近律）。

## 5. 圆角（**仅 5 档**）

`4`（标签／徽章）· `8`（按钮／输入框／菜单项）· `12`（卡片／面板）· `16`（对话框／大卡）· `9999`（药丸／头像）。

## 6. 阴影（4 层·暗色下换描边或 glow）

`--shadow-sm: 0 1px 3px rgba(0,0,0,.08)`｜`--shadow-md: 0 4px 12px rgba(0,0,0,.10)`｜`--shadow-lg: 0 12px 32px rgba(0,0,0,.14)`｜`--shadow-focus: 0 0 0 3px var(--accent-soft)`。

## 7. 动效

**150ms**（微交互）／**250ms**（组件与弹层切换）／**400ms**（页面过渡）；缓动统一 `cubic-bezier(.4,0,.2,1)`。**禁止**弹跳/旋转/脉冲。

## 8. 组件基线

| 组件 | 关键值 |
|---|---|
| **按钮** | 主：`--accent` 底＋白字，圆角 8，min 高 **36**；次：描边＋透明底；小：高 **28**／圆角 **8**；**热区一律 ≥44×44** |
| **输入框** | 圆角 8，边框 1px `--border`，聚焦 `--accent`＋`--shadow-focus`，占位 `--text-disabled`，错误态 `--danger` |
| **卡片** | 圆角 12，`--shadow-sm`，内边距 24（紧凑 16） |
| **对话框** | 宽 440/560/720，圆角 16，遮罩 `rgba(0,0,0,.5)` |
| **表格** | 表头加粗＋`--bg` 底，行高 40，隔行 `--bg`，单元格 8×12，窄屏横向滚动 |
| **侧栏** | 宽 240/280，底 `--bg`，当前项**左 3px `--accent` 指示条**＋`--surface` 悬停 |
| **审批卡** | **专件**：`审批卡-设计规格-v1.0-20260919.md` |

## 9. 九态与无障碍（**底线**）

每组件 **default／hover／active／focus／disabled／loading／empty／error／success 九态齐**；键盘可达＋**焦点可见**；反馈 <400ms，>1s 给 loading，>10s 给进度；空态**必须有引导文案**（不得留白）。

## 10. 迁移与验收（**四条可机判**）

1. `src/styles/` 内 **裸 hex ＝ 0**（`#[0-9a-fA-F]{3,8}` 命中为 0）；
2. **圆角取值 ⊆ {4,8,12,16,9999}**；
3. **字号 ⊆ {11,13,15,18,22,28}**；
4. **间距全为 4 的倍数**（`padding|margin|gap` 不含 5/7/10/13/14/15/18px 等）。

---

## 附一：项目注记（本仓落地记录·2026-09-19）

1. **落地位置**：本仓根 `DESIGN.md`（分支 `integrate/web-20260919`）。token 实体在 `src/styles/core.css` 的 `:root` 与 `:root[data-theme='dark']` 两块；**除这两块外，`src/styles/**` 内不再出现裸 hex**。
2. **§一#8 二选一（已决）**：亮/暗**统一走「冷调蓝灰」**（照本件 §2 值）；**弃** VS Code 中性灰（`#252526`/`#e8e8e8` 那套）。
3. **§一#7 张力（已决）**：**主按钮 36 / 次按钮 36 / 小按钮 28**；**交互热区一律 ≥44×44**——用 `::after` 伪元素扩热区，**不撑视觉高度**。
4. **徽章文字色（补偿条目）**：§2 只定了徽章**底色**＝`var(--risk-*)` ＋10%；为满足 §10 护栏「对比度 ≥4.5:1」，**徽章文字另配** `--risk-high-text`／`--risk-medium-text`（值见 `core.css`，实测比值见 `MSG-3145` 讫报）。
5. **暗色主按钮文字色（补偿条目）**：暗色 `--accent`（`#818CF8`）配白字对比度不足，**暗色 `--accent-contrast` 取深墨 `#0F1117`**（实测比值见讫报）——本件 §8「白字」在暗色下按此例外执行。
6. **动效书写归一**：`0.15s` 与 `150ms` 同值，统一写作 `150ms`；**删除** `120ms`（微交互并入 150）／`0.22s`→`250ms`；脉冲类关键帧按 §7 **禁止脉冲** 条撤除（进度/运行态改「常量点＋文案」表达）。
7. **等宽字体**：本仓既有 `'Cascadia Code', Consolas, monospace` 栈与本件 §3 一致，保留。
8. **本文档内部冲突照录（已决·取「可机判」项）**：§4 写「按钮内边距 8×16（小号 **6×12**）」、审批卡规格 §二 写「徽章内边距 **2×8**」——二者与 §10#4「间距全 4 倍数」冲突。**取可机判项**：小号按钮 **8×12**、徽章 **4×8**（视觉差 2px，肉眼不可辨），并在 `src/styles/**` 与 `ApprovalCard.vue` 内以注释留痕。
9. **档位选中态**：审批卡规格 §二 未定选中底色。实测「`--accent` 文字 ＋ `--accent-soft` 底」在暗色下仅 **4.40:1**（不达 §10 护栏）⇒ 选中态取 **`--accent` 实心 ＋ `--accent-contrast` 文字**（亮/暗皆 ≥6.29:1）。另：`--success-text` 亮色取 `#166534`（`#15803D` 配 `--success-soft` 底实测 4.50:1，贴线不合格）。
10. **动效例外（照录）**：全仓动效已归一到 150/250/400ms，**唯一例外**＝流式光标 `blink 1s steps(1)`（文本插入符语义，非装饰脉冲）；审批卡 loading 转圈取 **400ms**（§7 禁旋转与卡规格 §三「转圈」冲突处，取专件规格）。

## 附二：本次落地范围（与《界面审美体检 v1.0》§一 对应）

| 体检 §一 条目 | 落地动作 |
|---|---|
| 1 无强调色／`--link` 另起一套 | `--accent` 恢复 `#4F46E5`／`#818CF8`；删 `--link`，链接与选中态随 `--accent` |
| 2 裸 hex 147+ | 全量 token 化（新 token 补进 `core.css`） |
| 3 圆角 12 种 | 归一到 5 档 |
| 4 间距非 4 倍数成片 | 归一到 4 倍数（阶梯优先） |
| 5 字号 11 档缺 h1 | 归一到 6 档（含 28 h1 档） |
| 6 动效只有 120/150 | 删 120，取 150/250/400 |
| 7 按钮高 26/28/32/34 | 主 36／小 28；热区 ≥44×44 |
| 8 亮暗气质不一致 | 统一冷调蓝灰（见附一#2） |

---

*拟：参谋窗（用 `ui-designer` skill）｜2026-09-19｜本仓落地：前端窗 MSG-3145*
